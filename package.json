{
  "name": "liveroo_app",
  "version": "0.1.0",
  "private": true,
  "description": "Liverooo! - Team lunch ordering application",
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "start": "cd web && npm start",
    "build": "cd web && npm run build",
    "test": "cd web && npm test",
    "install-all": "npm install && cd web && npm install",
    "dev": "cd web && npm start",
    "api": "cd api && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
